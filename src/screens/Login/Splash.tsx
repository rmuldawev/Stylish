import {Box, Container, Text} from 'native-base';

const Splash = () => {
  return (
    <Box safeAreaTop alignItems={'center'} flex={1} bg={'red.900'}>
      <Text>SplashScreen</Text>
    </Box>
  );
};

export default Splash;
